# Effort 27 — CUDA DECODE: close the llama.cpp gap on the RTX 4090 (MoE 31–42%, dense 75–91%)

> **Status:** 🔬 OPEN (spawned 2026-06-14). Runs on the **RTX 4090**
> (`GPU-e59a6fce-1961-bafe-927c-06c0149f2370`). The parallel **Effort-26 BEAT_LLAMA**
> loop owns the **5090** (`GPU-5126d018-…`) + `~/Workspace/zinc-e26` + box dir
> `~/zinc-e26` — do NOT touch them, the main checkout `~/Workspace/zinc`, or push to
> main. Work in THIS worktree + box dir `~/zinc-e27`.
> Forward paths: `src/compute/forward_cuda.zig` (qwen35/36 hybrid-SSM dense + MoE),
> `src/compute/forward_cuda_gemma.zig` (gemma4 dense + MoE), `src/shaders/cuda/kernels.cu`.

## The gap (5090 catalog 2026-06-13, zinc decode as % of llama — same shape on the 4090)
- **gemma-4-26B-A4B MoE — 31%** of llama (47.5 vs ~153) ← worst, the headline lever
- **qwen36-35B-A3B MoE — 42%** (52.9 vs ~126)
- qwen35-9B dense — 75% (120.8 vs ~161)
- gemma-4-31B dense — 82% (46.9 vs ~57)
- qwen36-27B dense — 91% — fine, leave it
4090 decode is **launch/latency-bound** (Effort-25 proved graph replay buys ~8–12% on
small dense; size-gated). llama's years-tuned gathered-expert matvecs lead on MoE.

## PRIMARY — MoE decode (the 31–42% gap)
e26/5090 is doing PREFILL (all its cycles so far), so **MoE-decode is unclaimed in
practice — own it here.** Per-token MoE decode = router norm→gate matvec→top-k, then 8
routed-expert matvecs (Q4_K gate/up + Q5_1 down) + Q8_0 shared expert + weighted combine,
per layer (gemma-26b: 30 MoE layers; qwen-35b-a3b: most). Levers:
- **PROFILE FIRST** (util/clock + per-op): launch-bound (many tiny launches/token → GPU
  idles) vs expert-matvec-bound. Let the profile pick the cycle's change.
- **Fuse the per-token expert path** (router+experts+shared+combine = many small
  launches/layer); the Effort-24 batched-prefill expert kernels are twins — the
  single-token decode path may still be launch-heavy.
- **Expert-matvec efficiency**: `dmmv_q4k`/`dmmv_q5_1` for the 8 active experts vs llama's
  gathered-expert matvec (coalescing, dp4a, block size).
- **qwen36 MoE async-pipelining** was a WIN (35b-a3b ~1.5×, branch `perf/moe-async-decode`)
  — check if it's on current main; if not, re-validate + land. **Do NOT async GEMMA decode
  (boost-saturated, proven regression); qwen MoE async is fine.**

## SECONDARY — dense decode fusion (Effort-23 playbook; proven repeatable on this box)
qwen35-9b 75%, gemma-31b 82%. Effort-23 landed 4 stacked dense-decode fusion wins
(V/KV-write, output-scale fold, Q4_K matvec-pair, qkv-norm) on branches **pushed NOT main**
(`perf/e23-*`). Lever: **aggregate ≥2 tiny per-token launches per fused kernel** to clear
this box's ±1% boost floor (single-launch fusions need locked clocks — no
`nvidia-smi -lgc` w/o sudo here). Re-validate + land the unmerged e23 wins on current main
where they still apply, or find new ones. Also: LM-head matvec (vocab×n_embd, biggest
read/token).

## HARD RULES (override the generic playbook)
- **Pin the 4090:** `export CUDA_VISIBLE_DEVICES=GPU-e59a6fce-1961-bafe-927c-06c0149f2370`;
  run validate_catalog + all measurements with `ZINC_GPU=GPU-e59a6fce-1961-bafe-927c-06c0149f2370`.
- **NEVER** touch the 5090 (`GPU-5126d018-…`), `~/Workspace/zinc-e26`, box `~/zinc-e26`,
  the main checkout `~/Workspace/zinc`, or push to main. Box build dir = `~/zinc-e27`
  (rsync source there; never `~/workspace/zinc`).
- Isolated-cache builds (`ZIG_LOCAL_CACHE_DIR`+`ZIG_GLOBAL_CACHE_DIR`; verify the binary
  md5 changed or you measure stale code).
- **Gate:** `scripts/validate_catalog.sh` (ZINC_GPU=4090) MUST stay 5/5 token-correct
  (fused kernels bit-equivalent); if correctness breaks → REVERT + document.
- **Measure interleaved back-to-back A/B** (4090 decode is boost-noisy); compare zinc decode
  tok/s vs the pre-cycle binary AND vs llama on the SAME 4090 + same gguf
  (`~/workspace/llama.cpp/build/bin/llama-bench` or the perf suite). Never trust one boosted run.
- **VALIDATED WIN** → commit ONLY that change to `perf/e27-<short-target>`, push (NOT main),
  append a dated cycle-log entry here + to memory. **NEGATIVE** → revert code, log the finding.
- Box gotchas: tok/s prints to STDERR (`2>&1`); `nohup CMD >FILE 2>&1 &` + poll FILE;
  util-gate via `--query-gpu=utilization.gpu`; gemma reloads ~18GB/call.

## CYCLE LOG
- **CYCLE 19 (2026-06-15) — ✅ VALIDATED WIN: fuse the gemma-26b MoE decode post-attn norm+residual + the 3 MoE pre-norms into ONE single-block launch → decode +3.5% (branch `perf/e27-moe-attn-prenorm-fuse` commit `31448076`, based on C17's `perf/e27-moe-norm-combine`, pushed NOT main).** REFUTES C18's "launch-fusion space confirmed truly EXHAUSTED" — there was ONE more cross-command-boundary single-block adjacency of the C8/C11/C17 win-class. On the MoE decode path the attention block ends with `rms_norm_residual` (post-attn norm + residual → `hidden`, one single-block launch) and `moeFfnBlock` then OPENS with `rms_norm_triple` (the 3 pre-norms — ffn_norm/router-noweight/pre_ffw_norm_2 — off the just-updated `hidden`, another single-block launch) across a command boundary. Both are single-block n_embd reductions over the SAME `hidden`, so fuse them. **Change (`kernels.cu` +58 = new `rms_norm_residual_triple`; `forward_cuda_gemma.zig` +18/−1):** phase 1 = the exact `rms_norm_residual` arithmetic (Σx²→rinv, `hidden[i]+=w_post[i]*(x[i]*rinv)`); a `__syncthreads` makes the just-written `hidden` visible; phase 2 = the exact `rms_norm_triple` arithmetic re-reading `hidden` (Σhidden²→rinv2, emits `y1=w1*xr`, `y2=xr`, `y3=w3*xr`). **BIT-IDENTICAL** to the two-kernel path (phase 1 reproduces `rms_norm_residual` byte-for-byte, phase 2 reproduces `rms_norm_triple` byte-for-byte; intervening `__syncthreads` make the `zinc_block_reduce_sum` scratch reuse race-free — the `rms_norm_residual_norm` pattern). Single-block like BOTH originals → in-flight block-count PRESERVED (the C8/C11/C17 win-class, NOT C10's collapse); removes ONE tiny launch + the `hidden` store/reload round-trip per MoE layer (~30 launches/token on gemma-26b). Opt-in behind `ZINC_ATTN_MOE_NORM` (`fold` stays false on MoE layers → the `else if (self.fuse_attn_moe_norm)` branch fires in the attention block, and `moeFfnBlock` skips its standalone `rms_norm_triple`); default OFF = the C11 two-launch path. **Correctness PASS: `scripts/validate_catalog.sh` 5/5 token-correct BOTH default OFF (gemma4-26b 12/12) AND `ZINC_ATTN_MOE_NORM=1` (gemma4-26b 12/12 fused; qwen 12/12 untouched — gemma forward only; gemma4-31b the documented teacher-forced near-tie); gemma-26b GEN_IDS byte-IDENTICAL OFF vs ON over 40 tok.** **A/B 4090-pinned interleaved, gemma-26b NGEN=300, OFF=C11 default vs ON=`ZINC_ATTN_MOE_NORM=1`, 22 rounds across 2 batches (batch2 order-alternated; e28 5090-pinned, intermittent box contention → several fail-to-boost rounds in the low-50s on BOTH arms):** combined paired ON wins **14/22** (64%), paired median ratio **+2.9%** / mean +3.95%, abs medians 61.2→63.4 tok/s (+3.5%); **CLEAN-only (both arms boosted >60, removes the fail-to-boost noise on whichever arm randomly dipped) = 8/10 cyc wins (80%) at +3.65% median** — the C8/C11/C17 win-class signature (positive abs median + high clean win-rate, magnitude class of C11's +3.3%). **vs fresh llama-bench tg128 = 141.33 ± 1.83 t/s** same 4090+gguf → clean-window gemma-26b decode ~48% (OFF ~67) → ~50% (ON ~68-73) of llama, incremental over the C11 default triple. **CONCLUSION: VALIDATED WIN → committed + pushed.** **LESSON: C18's "fusion space truly EXHAUSTED" was PREMATURE — it scanned the routed-experts→combine TAIL but missed the attention→MoE-block BOUNDARY at the layer HEAD. The C8/C11/C17 win-class (fuse adjacent single-block rms-style ops across a command boundary, preserve block-count) had one more member: the post-attn norm+residual and the opening triple. With it landed, the gemma-26b MoE layer head is also fused (post-attn + 3 pre-norms now ONE launch). Scan ALL command boundaries (head AND tail), not just the tail, before declaring fusion done. Residual ~50% gap stays the expert-matvec weight-traffic wall (C5/C6/C13/C14/C15, dp4a REFUTED for already-4-bit Q4_K).** Box scratch cleaned; e28/5090 left undisturbed.
- **CYCLE 18 (2026-06-15) — ❌ LOGGED NEGATIVE (code REVERTED, tree clean, doc-only on `perf/e27-moe-norm-combine`): isolating the gemma-26b MoE decode `zero_vec`-drop (skip the routed-experts accumulator pre-clear, OVERWRITE instead of `+=`) is NOT a validated win — cyc fails to beat base (the C10/C13/C14 fail-to-boost pattern), refuting the C17-lesson hypothesis that C12's GeGLU-fold `tanhf` was MASKING a zero_vec-drop gain.** Followed C17's directive ("scan for OTHER cross-command-boundary single-block-op adjacencies before declaring fusion exhausted") + the C12 post-mortem (the GeGLU+zero_vec BUNDLE was neutral, with the GeGLU fold's per-block `tanhf` blamed for the 4 fail-to-boost rounds). Hypothesis: **isolating** the zero_vec-drop (no GeGLU-fold confound) might surface the small gain the bundle's +1.36% paired median hinted at. **Change (`kernels.cu` +14 = new `moe_weighted_acc_scaled_ow`, a byte-for-byte copy of `moe_weighted_acc_scaled` but `a[i]=sum` OVERWRITE not `a[i]+=sum`; `forward_cuda_gemma.zig` +10/−3):** the batched routed-experts decode branch in `moeFfnBlock` already writes every `moe_out_buf[i]` exactly once over `[0,N)` via the single-launch weighted combine, so `a[i]=sum` makes the separate `zero_vec(moe_out_buf)` pre-clear redundant (`0+sum==sum`) → skip that launch. Opt-in behind `ZINC_MOE_ZERO_DROP` (default OFF → byte-identical to the pre-cycle binary; ONE binary, env-toggle A/B = the C3/C16/C17 zero-binary-diff method); scoped to the batched decode path (`moeRoutedCombine` per-slot/prefill keeps its own `zero_vec`). Block-count of the acc launch PRESERVED (the C8/C17 win-class, NOT C10's collapse) — removes ONE whole tiny launch/MoE layer. **Correctness PASS: `scripts/validate_catalog.sh` 5/5 token-correct BOTH default (gemma4-26b 12/12) AND `ZINC_MOE_ZERO_DROP=1` (gemma4-26b 12/12 on the overwrite path; qwen 12/12 untouched, gemma forward only; gemma4-31b the documented teacher-forced near-tie); gemma-26b GEN_IDS byte-IDENTICAL OFF vs ON over 40 tok (bit-exact).** **A/B 4090-pinned interleaved, SAME binary (md5 `9ec74130`), base(env unset) vs cyc(`=1`), gemma-26b NGEN=300, order-ALTERNATED per round (R-odd base→cyc, R-even cyc→base, to control for time-drift). e28 was HEAVILY contending the box (its `cuda-dbg batch` + ~19GB-RSS gemma reloads leaking onto the 4090 — ALL rounds tagged CONTENDED, no clean window forthcoming):** 4 rounds ratios {0.8951, 0.7825, 0.9004, 1.3488} → **cyc wins only 1/4, and that round (R4=1.3488) is BASE dipping to 51.03 (fail-to-boost in the base arm), NOT cyc rising (cyc 68.83 sits inside the shared 51.7–68.8 band); abs medians base ~64.3 → cyc ~58.6 (~9% slower).** Both arms span the same 51–68 band; cyc is below base in 3/4 properly-interleaved rounds with the order flipped — so cyc-slower is NOT a contention-order artifact (R2 ran cyc FIRST and cyc was still slower). **NONE of the C7/C8/C17 clean-win signature (tight high cyc band + positive abs median + high win-rate). Stopped the A/B early** (e28 is "super important" + the verdict was already unambiguous + no clean 4090 window was forthcoming). **NEUTRAL-to-NEGATIVE → NOT a validated win → reverted.** **MECHANISM:** `zero_vec(moe_out_buf)` is a tiny ~36-block launch; removing it gives the boost-sensitive gemma-26b MoE decode even LESS work between the big expert matvecs, and on this box that perturbs boost-ramp (fail-to-boost) more than the saved launch-bubble buys — the C10/C13/C14 signature (ANY perturbation of this path regresses). The win-class needs the removed launch to sit on a genuinely EXPOSED inter-large-launch bubble ON the dependency-chain tail (C8 combine 3→1, C17 post_ffw_norm_2+combine 2→1 both removed rms-style work on the chain tail); `zero_vec` is an off-chain pre-clear that overlaps cheaply → removing it recovers no real bubble. **LESSON (refutes the C17-lesson hypothesis, re-closes the fusion arc): C12's GeGLU+zero_vec bundle-neutral was NOT masking a zero_vec-drop win — isolated zero_vec-drop is itself non-winning; C12's neutral verdict was correct for BOTH components. The C17 `post_ffw_norm_2`+combine pair was the LAST genuinely-winning cross-command single-block adjacency; the gemma-26b MoE decode launch-fusion space is now confirmed truly EXHAUSTED. Residual ~40% gap = the expert-matvec weight-traffic wall (C5/C6/C13/C14/C15, dp4a REFUTED for already-4-bit Q4_K — the hard rewrite with no cheap entry).** CAVEAT: measured under heavy e28 contention (no clean 4090 window); the ratio metric (interleaved, order-alternated) is robust to boost-warming and still shows no win, and the direction is corroborated by C12 + the C10/C13/C14 precedent. Box scratch cleaned (isolated caches + logs removed, box source reverted); e28 left undisturbed.
- **CYCLE 17 (2026-06-15) — ✅ VALIDATED WIN: fuse the gemma-26b MoE decode `post_ffw_norm_2` + combine tail into ONE single-block launch → decode +7–10% (branch `perf/e27-moe-norm-combine` commit `e10e6124`, based on C11, pushed NOT main).** RE-OPENED the "static-fusion exhausted" claim (C12) and found a missed BLOCK-COUNT-PRESERVING candidate of the C8/C11 win-class that C12 never enumerated: on the routed-experts → combine boundary, `moe_out_buf` is rms-normed by `post_ffw_norm_2` (`rms_norm(moe, wpn2)`, ONE single-block launch, end of the routed-experts command) and then immediately consumed by `moe_combine_tail` (`hidden += post_ffw_norm(shared + moe)`, ANOTHER single-block launch in a SEPARATE command). Two adjacent single-block rms-style launches across a command boundary — exactly C8/C11's "fuse adjacent single-block ops" pattern, never applied here (C9 did the Q8_0 shared dual, C10 the router pre-norm, C12 GeGLU+zero — none touched this pair). **Change (`kernels.cu` +47 = new `moe_norm_combine_tail`; `forward_cuda_gemma.zig` +32/−4 across BOTH decode combine sites — `moeFfnBlock` batched/decode + `moeRoutedCombine` fallback):** the fused kernel reads `moe` RAW (the weighted-acc, NOT pre-normed) — phase 1 reduces `ss=Σmoe²`→`rinv1=rsqrt(ss/N+eps)` (== `rms_norm(moe,wpn2)`); phase 2 forms `t = shared + wpn2*(moe*rinv1)` and reduces `ss2`→`rinv2` (== `moe_combine_tail`'s reduction over the post_ffw_norm_2 output); phase 3 writes `hidden += wpost*(t*rinv2)`. **BIT-IDENTICAL:** the normed-moe `wpn2[i]*(moe[i]*rinv1)` is RECOMPUTED in phases 2+3 (never written back, like `moe_combine_tail` recomputes `t`), so it matches `rms_norm`'s f32 output byte-for-byte, and `t`/`ss2`/`rinv2`/the hidden update are byte-for-byte `moe_combine_tail`'s; intervening `__syncthreads` make the `zinc_block_reduce_sum` scratch reuse race-free (the `rms_norm_residual_norm` pattern). Removes ONE launch + the `moe_out_buf` store/reload round-trip; single-block like both originals → in-flight block-count PRESERVED (the C8/C11 win-class, NOT C10's 36→1 collapse). Opt-in behind `ZINC_MOE_NORM_COMBINE` (default OFF → byte-identical to the pre-cycle binary; ONE binary, env-toggle A/B = the C3/C16 zero-binary-diff method). **Correctness PASS: gemma-26b GEN_IDS token-IDENTICAL OFF vs ON over 40 tok (the fusion is bit-exact); full `scripts/validate_catalog.sh` 5/5 token-correct WITH `ZINC_MOE_NORM_COMBINE=1` (qwen35/36 12/12 — untouched, gemma forward only; gemma4-26b 12/12 on the fused path; gemma4-31b the documented teacher-forced near-tie).** **A/B 4090-pinned interleaved, SAME binary (md5 `50ce23fc`), base(env unset) vs cyc(`=1`), gemma-26b NGEN=300, e28 SHARING the 5090 (its `cuda-dbg batch` is 5090-pinned; the 4090 saw intermittent foreign-compute snapshots tagged CONTENDED):** batch1 (10 rounds) {1.0493,1.1123,1.1334,0.9621,1.0413,1.1032,1.1247,1.0161,1.4339,1.0443} → 9/10 cyc wins, CLEAN-only (R2–R6, both GPUs idle) median **+10.3%** / abs medians 51.27→56.35 (+9.9%); batch2 (10 rounds, warm ~63–74 tok/s) {1.0676,1.1051,1.0761,1.0662,1.0745,1.0323,0.9607,1.0803,1.0191,1.0920} → 9/10 cyc wins, median **+7.1%** / abs 67.4→71.4 (+5.9%). **Combined 20 rounds: 18/20 cyc wins; CLEAN-only (6 rounds) median +9.8%.** Tight high cyc band + positive abs medians + high win-rate in BOTH boost regimes = the C7/C8/C11 win signature, well above the ±1% boost floor and above C12's rejected +1.36%. **vs fresh llama-bench tg128 = 142.13 ± 3.31 t/s** same 4090+gguf (warm, e28 sharing) → matched-warm-regime gemma-26b decode ~47% (base 67.4) → ~50% (cyc 71.4) of llama; the robust validated metric is the relative +7–10% vs the pre-cycle binary. **CONCLUSION: VALIDATED WIN → committed + pushed.** **LESSON (re-opens then re-closes the C12 "static-fusion exhausted" claim): C12's enumeration was INCOMPLETE — it missed the `post_ffw_norm_2`+combine pair (a cross-command-boundary adjacent single-block fusion). The C8/C11 win-class (fuse adjacent single-block rms-style ops, preserve block-count) had ONE more member. With it landed, the gemma-26b decode tail is now ~4 fused launches leaner per MoE layer (C8 combine 3→1, C11 3 pre-norms→1, C17 post_ffw_norm_2+combine 2→1). The residual gap stays the expert-matvec weight-traffic wall (C5/C6/C13/C14/C15, the hard rewrite, REFUTED for dp4a on already-4-bit Q4_K). Future scans should look for OTHER cross-command-boundary single-block-op adjacencies before declaring fusion exhausted.** Box scratch cleaned; e28/5090 left undisturbed.
- **CYCLE 16 (2026-06-15) — ❌ LOGGED NEGATIVE (code REVERTED, tree clean, doc-only commit): porting the C7-class routed gate+up static-fusion to the QWEN36-35b-a3b MoE decode path — the one genuinely-untried application of the proven gemma lever (C7/C8/C11 were all gemma-only) — nets only ~+1% (paired median +1.0%, clean-only +1.2%, 12/15 cyc wins), AT the boost floor and BELOW the C12 +1.36% that this effort already rejected as in-noise. The result SYMMETRICALLY CLOSES the static-fusion question: the same fusion wins +8% on graph-less gemma (C7) but is in-noise on qwen because the C2 CUDA graph ALREADY DRAINED the launch bubble.** Pivoted off the (exhausted, C5-C15) gemma-26b MoE to the highest-value un-attacked target: qwen36-35b-a3b is the closest-to-llama MoE (92% via C1+C2+C3) and its routed gate+up matvecs (`forward_cuda.zig` ~889/891) read the SAME `ffn_norm_buf` as TWO separate `dmmv_q4k_experts` launches — exactly the C7 win-pattern, never applied to qwen. **The open question: does C7-class static fusion still help a model that's ALREADY CUDA-graph-captured (C2), or did the graph drain the launch bubble?** Unlike gemma (gate+up are one FUSED tensor at a base offset → `dmmv_q4k_experts_dual`), qwen's gate (`ffn_gate_exps`) and up (`ffn_up_exps`) are SEPARATE Q4_K tensors, so I built a **two-weight-buffer variant `dmmv_q4k_experts_dual2`** (`kernels.cu` +73; reads x's float4s ONCE per superblock shared between the gate+up accumulators, weights from the two distinct buffers at the identical block address; each accumulator's dequant byte-for-byte `dmmv_q4k_experts` → bit-identical). Wired into `forward_cuda.zig`'s `moeFfnBlock` batched branch behind `ZINC_MOE_GU_DUAL` (default OFF → default path byte-identical to pre-cycle; ONE binary, env-toggle A/B = the C3/C4 zero-binary-diff methodology), engaging only when both gate+up are Q4_K (the 1 Q5_K-gate/up layer of 40 keeps the two-launch path). **Correctness PASS: `scripts/validate_catalog.sh` 4/5 (all 3 qwen 12/12 incl. qwen36-35b-a3b on the default path; gemma4-26b 12/12; gemma4-31b the documented near-tie — UNAFFECTED, it runs the untouched `forward_cuda_gemma.zig` and validate ran with the env unset = base behavior); AND qwen36-35b-a3b GEN_IDS byte-IDENTICAL dual-OFF vs dual-ON over 40 tok (the fusion is bit-exact).** **A/B 4090-pinned interleaved, SAME binary, base(`ZINC_MOE_GU_DUAL` unset) vs cyc(`=1`), qwen36-35b-a3b NGEN=300, e28 SHARING the 4090 (its `~/zinc-e28` `cuda-dbg batch` loop — NOT dedicated):** batch1 (10 rounds, e28 resident keeping the GPU boost-WARM ~105 tok/s) ratios {1.025,1.144,0.987,1.083,1.033,0.977,1.007,1.005,1.010,1.004} → 8/10 cyc wins, paired median +0.9%, abs medians cyc 107.4/base 105.6; batch2 (5 CLEAN rounds, e28 idle → lower idle-boost ~90 tok/s) {1.012,1.009,1.031,1.050,0.971} → 4/5 cyc wins, median +1.2%, cyc tight 89.4–91.6 entirely above base's wider 86.5–92.0. **Combined 15 rounds: 12/15 cyc wins, paired median +1.0%, clean-only +1.2%.** The win SIGNATURE is present (tight high cyc band + positive abs median + high win-rate — what C12 LACKED) but the MAGNITUDE is at the ±1% boost floor and below C12's rejected +1.36%. **CONCLUSION: NEUTRAL/marginal → NOT a validated win by this effort's own C12 precedent → reverted.** **MECHANISM (the valuable part): the C2 CUDA graph already removed the CPU-side per-launch overhead, so fusing gate+up only saves (1) the GPU-side grid-launch latency of one kernel and (2) one read of the small/cached `ffn_norm_buf` (x_stride=0, shared across experts) — both marginal. On graph-LESS gemma (C7) the SAME fusion recovered the FULL exposed inter-launch bubble = +8%; under a graph that bubble is already gone, leaving ~+1%.** **LESSON (symmetric closure of the static-fusion arc): static launch-fusion and CUDA graphs are SUBSTITUTES, not complements, for the launch-bubble lever — a model captured by a graph (qwen C2) has its bubble already drained, so adding the C7/C8/C11 fusion on top nets in-noise; the fusion is the right tool ONLY where graphs are absent/forbidden (gemma decode, where graphs are neutral-to-negative per C4). With qwen graph-captured (92%) and gemma fusion-harvested (~63%) + matvec-walled (C5-C15), BOTH e27 MoE models are now at their cheap-lever ceiling from BOTH directions (graph OR fusion). The residual gaps are the hard weight-traffic/gathered-expert wall (a prefill/L2 property decode's one-token-per-expert can't capture, C15) — no cheap decode lever remains across the catalog.** Box scratch cleaned; e28 left undisturbed.
- **CYCLE 15 (2026-06-15) — ❌ LOGGED NEGATIVE (code REVERTED, tree clean, doc-only commit): the dp4a/int8 expert-matvec rewrite — THE effort's last-theorized "only remaining winning direction" — REGRESSES gemma-26b MoE decode ~−15% clean, and the REASON REFUTES the premise: dp4a does NOT cut weight bytes for already-4-bit Q4_K weights.** Picked up an in-progress, uncommitted, unvalidated C15 attempt a prior session left in the working tree (the dp4a rewrite C5/C6/C13/C14 all pointed to as the sole untested lever) and carried it to a full verdict. **Change (`kernels.cu` +110, `forward_cuda_gemma.zig` +25, all OPT-IN behind `ZINC_MOE_DP4A`):** new `quantize_q8_1` (a row of `n` floats → per-32-block symmetric int8 `qs` + `float2 ds=(d=amax/127, Σx=d·Σq)`, mirrors llama `block_q8_1`) + new `dmmv_q4k_experts_dual_dp4a` (the C7 routed gate+up dual, but the per-nibble fp dequant-MAC replaced by int8 `__dp4a` against the q8_1 activation; per natural Q4_K sub-block `d·sc·xd·Σ(nibble·xq) − dmin·min·Σx`, `get_scale_min_k4` 6-bit unpack). The decode `moeFfnBlock` batched branch, when `ZINC_MOE_DP4A` is set, quantizes `moe_norm_buf` ONCE then dp4a's gate+up over all experts; else the proven fp `dmmv_q4k_experts_dual`. The **default path (env unset) is functionally identical to the pre-cycle binary** (new buffers/pipelines allocated-but-unused, the `if` takes the original else-branch) → I built ONE binary and A/B'd via the env toggle (zero binary-diff noise, the C3/C4 methodology). I VERIFIED the dp4a Q4_K layout math against both the canonical llama Q4_K storage AND the fp reference `dmmv_q4k_experts` before building (pair `p` reads weight bytes `[32p..32p+32)`, low nibbles → sub-block `2p`, high → `2p+1`, x-block `s*8+2p/2p+1`, scales `get_scale_min_k4(2p/2p+1)`). **Correctness PASS (NOT bit-exact — rides the tolerance gate): full `scripts/validate_catalog.sh` 5/5 BOTH with the default fp path AND with `ZINC_MOE_DP4A=1` — gemma-26b 12/12 token-IDENTICAL to llama on the dp4a path (md5 `80358f47`; gemma4-31b the documented teacher-forced near-tie).** So the int8 quantization drift is small enough the dp4a kernel produces the EXACT same greedy continuation — the math is right. **A/B 4090-pinned interleaved, SAME binary, base(`ZINC_MOE_DP4A` unset) vs cyc(`=1`), gemma-26b NGEN=300, contention-gated (foreign-4090-compute-app snapshots per round), 11 CLEAN rounds across two 8-round batches (e28 returned to the shared 4090 mid-run — contended rounds excluded):** ratios {0.7822, 0.9459, 0.9815, 0.8139, 0.8337, 0.7782, 0.9674, 0.8497, 1.0509, 0.8509, 0.9344} → **paired median 0.8509 (~−15%); abs medians base 62.67 → cyc 51.30 tok/s (~−18%); cyc wins only 1/11 CLEAN rounds — and that one (ratio 1.0509) is BASE dipping to 48.57 (its lowest reading), not cyc rising (cyc 51.04 is mid-band).** In ~half the clean rounds cyc is pinned ~47-51 while base reaches 58-66 — the C5/C6/C13/C14 bimodal fail-to-boost signature, the INVERSE of the C7/C8/C11 tight-high-band win. base ~62.67 ≈ 58% of llama (C13/C11 tg128 = 108.69 same 4090+gguf). **CONCLUSION: NEGATIVE → reverted.** **MECHANISM (the important part — it REFUTES the effort's standing thesis): dp4a here does NOT cut the binding resource.** The Q4_K weights stay 4-bit packed nibbles in DRAM and the dp4a kernel reads the EXACT SAME nibble bytes (`int w = q4[i]`, 32 bytes / 64 weights = 4 bits/weight, identical to the fp kernel's `qs0/qs1` reads). dp4a only (1) reads the ACTIVATION as int8 instead of fp32 float4 — but the activation is the tiny/cached operand (~11 KB, shared across all experts via `x_stride=0`), a negligible traffic saving; (2) swaps per-nibble fp dequant+FMA for int8 MAC — an ALU/dependency-latency change; (3) ADDS a `quantize_q8_1` launch per layer (extra launch + DRAM round-trip for `qs`/`ds`). Since this matvec is WEIGHT-DRAM-traffic/latency-bound (C5/C6) and the weights are ALREADY maximally packed at 4-bit, dp4a addresses the WRONG resource (ALU/activation, not weight bytes), while adding a launch and perturbing the boost-sensitive occupancy → the fail-to-boost regression. **The earlier roadmap premise ("dp4a cuts weight→int8 dot") was subtly WRONG for already-4-bit Q4_K weights: dp4a's weight-byte savings only materialize when you START from fp16/fp32 weights (a dense fp GEMM); for Q4_K-packed weights there are no weight bytes left to save — `int8` is WIDER than a 4-bit nibble. dp4a is a COMPUTE optimization, and this kernel is not compute-bound.** **LESSON (the decisive close for gemma-26b MoE decode): there is NO weight-byte reduction available for the Q4_K routed-expert matvecs — they are already 4-bit, so the "cut weight bytes via dp4a/int8" lever (the last theorized winning direction across C5/C6/C12/C13/C14) is REFUTED, not merely hard. The only remaining real lever is a gathered/grouped expert LAYOUT for L2 residency across a token's experts (llama-style) — but at DECODE there is one token per expert, so there is no cross-token weight reuse to exploit (that lever is a PREFILL win, see e26's grouped-experts A/B). gemma-26b MoE decode is therefore at its lever ceiling on this box: the validated wins C7 (+8%) / C8 (+12%) / C11 (+4.6%) stand as the result, ~58% of llama, and the residual ~42% gap is a HARDWARE/algorithm wall (llama's gathered-matvec advantage is a prefill/L2 property that decode's one-token-per-expert traffic cannot capture). The cheap, medium, AND hard cheap-rewrite decode levers are now ALL exhausted/refuted for this model.** Box scratch cleaned.
- **CYCLE 14 (2026-06-15) — ❌ LOGGED NEGATIVE (code REVERTED, tree clean, doc-only commit): fusing the C7 `dmmv_q4k_experts_dual` two block-reductions (3 `__syncthreads` → 1) via a new `zinc_block_reduce_sum2` float2 block-reduce is a slight REGRESSION (~−5% clean, fail-to-boost signature).** Continued the C8/C11 block-count-PRESERVING static-fusion lever, but at the INTRA-kernel reduction-tail level: the C7-winning gemma-26b MoE routed gate+up dual kernel (`dmmv_q4k_experts_dual`, gathers gate base 0 + up base gu_half, one launch) ends with two back-to-back scalar `zinc_block_reduce_sum` separated by an explicit `__syncthreads()` (sh[] reuse) = **3 syncs in the reduction tail**. **Change (`kernels.cu` only, +26/−4 = new `__device__ zinc_block_reduce_sum2(float2)` + swap the two-call tail for one fused call):** each float2 component runs the IDENTICAL warp-reduce tree as the scalar reduce, but the cross-warp step shares ONE `__shared__ float2 sh2[32]` + ONE `__syncthreads` → **3 syncs → 1**. **BIT-IDENTICAL** (each component's reduction is byte-for-byte the scalar form; only the sync schedule changes; scoped to the one dual kernel, all other matvec/dense paths untouched). **Correctness PASS: full `scripts/validate_catalog.sh` 5/5 token-correct on the cycle binary (md5 `dbb5f69d` ≠ base `d03a9e81`; gemma4-26b 12/12 on the fused path; gemma4-31b the documented teacher-forced near-tie).** **METHOD NOTE (important): the PRIOR session left a runaway self-contending A/B (`/tmp/e27_ab_c14.sh`, kept re-spawning + a second copy) hammering the 4090 — its partial 5-round read (median ~0.93) AND the box's 69%-util reading were SELF-CONTAMINATED, not e28. Killed all leftover procs, confirmed both GPUs idle, then re-ran a contention-GATED A/B that tags each round CLEAN/CONTENDED by snapshotting foreign 4090 compute-apps at the round boundaries.** **A/B 4090-pinned interleaved, base(md5 `d03a9e81`) vs cyc(md5 `dbb5f69d`), gemma-26b NGEN=300:** **5 CLEAN rounds (R1-R5, both GPUs idle): ratios {0.8295, 1.3932, 1.0056, 0.9430, 0.9417} → paired median 0.9430 (~−5.7%); abs medians base 51.77 → cyc 49.29 tok/s (~−4.8%); cyc wins only R2 — and that round is BASE dipping to 46.97, not cyc rising (cyc's 65.44 is its lone boosted round); in R1/R4/R5 cyc is pinned ~48-49 while base reaches 52-58** — the C5/C6/C13 bimodal fail-to-boost signature, the INVERSE of the C7/C8/C11 tight-high-band win signature. **6 CONTENDED rounds (R6-R11, e28 returned to the 4090) corroborate under matched contention: ratios {1.441,0.875,1.068,0.852,1.002,0.985}, cyc not winning (median ~0.99; the one >1 is again base dipping).** **CONCLUSION: NEGATIVE → reverted.** **Mechanism:** `dmmv_q4k_experts_dual` dispatches at only **64 threads/block (2 warps)**, so its reduction tail was ALREADY cheap (a 2-warp cross-warp step) — removing 2 of its 3 syncs saves almost nothing, while the float2 variant holds BOTH accumulators live through the cross-warp step (a marginally heavier reduce: `float2 sh2[32]`, two warp-reduce trees before the single sync) and perturbs the schedule of a kernel whose binding constraint is concurrent-warp occupancy → boost ramp (C13), not reduction-tail sync count. A reduction-tail micro-fusion neither adds in-flight parallelism nor cuts weight traffic → can't win, slightly regresses. **LESSON (sharpens C13, closes the intra-kernel-reduction-fusion sub-lever): the C8/C11 "preserve block-count" rule is NECESSARY but NOT SUFFICIENT — fusing WITHIN a single small (2-warp) launch's reduction tail preserves block-count yet still loses, because the saved syncs are negligible relative to the boost-ramp sensitivity it disturbs. Static launch-fusion wins ONLY when it removes a WHOLE inter-launch bubble between large/parallel launches (C7/C8/C11), never when it micro-optimizes the tail of one already-small launch. Combined with C5/C6/C13, EVERY thread/register/sync-schedule perturbation of the gemma-26b expert matvecs has now regressed or gone neutral — the sole remaining winning direction stays cutting WEIGHT BYTES (dp4a/int8 on the packed nibbles), the hard multi-cycle rewrite. The cheap decode-lever space across all e27 models is confirmed EXHAUSTED.**
- **CYCLE 13 (2026-06-15) — ❌ LOGGED NEGATIVE (code REVERTED, tree clean, doc-only commit): `#pragma unroll 2` on the gemma-26b MoE routed expert matvec inner loops (the C5-endorsed "RAISE per-thread MLP" lever) is a slight REGRESSION (~−2% paired, −2.56% abs, 7/16 wins, C5/C6 fail-to-boost signature).** First did a full PROFILE-FIRST sweep of the two un-attacked targets, which CLOSED both for the launch-fusion lever with data: **(a) gemma-31b DENSE decode is COMPUTE/BW-SATURATED — sampled ~98-99% util / ~400 W / 2700 MHz during the decode window (34.78 tok/s, "sync-per-layer"), so it has NO recoverable launch bubble → fusion dead (confirms the effort's tentative "boost-saturated" label with a measurement, parallel to C4's gemma-26b-graph closure); AND its `attn_v.weight` is Q6_K (Q/K are Q4_K, dual-fused) so a Q+K+V triple is inapplicable anyway — gemma-31b dense fusion is DOUBLY closed. (b) Both gemma LM-head matvecs already use the FAST kernel: gemma-31b token_embd Q4_K→`dmmv_q4k_fast`, gemma-26b token_embd Q8_0→`dmmv_q8_0_fast` (dmmvIdx 3 < 4) — the effort's "LM-head" lever is already taken.** With dense-gemma fusion dead and gemma-26b MoE static-fusion EXHAUSTED (C12), the one remaining lesson-ENDORSED-but-untested lever was C5's "RAISE per-thread MLP (more loads in flight)" — distinct from the dead thread-geometry axis (C5 cut trip-count via float4, C6 cut threads). **Change (`kernels.cu` only, +12 comment lines / `#pragma unroll 2` on two loops): added `#pragma unroll 2` to the inner superblock loops of the two gemma-MoE-ONLY routed expert matvecs — `dmmv_q4k_experts_dual` (routed gate+up, ~3 iters/thread) and `dmmv_q5_1_experts` (routed down, ~11 iters/thread)** — to software-pipeline the DRAM-latency-bound weight loads (issue the next iteration's independent loads while this one reduces → more outstanding loads/thread → better latency hiding). **BIT-IDENTICAL** (accumulation order unchanged, only the issue schedule; scoped to the 2 MoE-expert kernels, shared `dmmv_q8_0_fast`/dense paths untouched): **`validate_catalog.sh` 5/5 token-correct on the cycle binary (md5 e3c48b01 ≠ baseline d6250ad7; gemma4-26b 12/12 on the changed path; gemma4-31b documented near-tie); gemma-26b GEN_IDS token-IDENTICAL base vs cycle (40 tok).** **A/B 4090-pinned interleaved, gemma-26b NGEN=300, TWO 8-round batches (16 rounds, both GPUs idle/uncontended):** batch1 ratios {0.968,1.022,1.406,1.249,1.181,0.872,0.838,0.773}; batch2 {1.014,0.790,0.775,0.996,0.963,1.050,0.787,1.219}; **combined 16-round paired median 0.9818 (~−1.8%), abs medians 63.02→61.41 tok/s (−2.56%), CYC wins only 7/16** with ~5-6 cyc fail-to-boost dips (ratios 0.77-0.79, cyc pinned ~47-50 while base held the ~60-73 high band) — the C5/C6 bimodal-fail-to-boost signature, the INVERSE of the C7/C8 clean-win signature (early R1-R5 looked like a win only because base happened to dip in R3-R5; the later rounds reversed). base ~63 ≈ 58% of llama (C11 fresh tg128 = 108.69 t/s same 4090+gguf). **CONCLUSION: NEGATIVE → reverted.** **Mechanism:** NVRTC at `-O3` already software-pipelines these small runtime-bounded loops, so the explicit `#pragma unroll 2` adds nothing on the load-scheduling side; what it DOES add is register pressure (the dual kernel already carries 4 float accumulators + a heavy dequant body; unrolling doubles live temps), lowering occupancy on the boost-sensitive gemma-26b decode → cyc fails to ramp boost in ~1/3 of rounds (pinned ~47-50 vs base ~63), and the lost occupancy/boost costs more than the (already-present) pipelining buys. **LESSON (refines C5/C6, closes the per-thread-MLP-via-unroll lever): the gemma-26b MoE expert matvec is occupancy/boost-RAMP-sensitive — ANY change that raises per-thread register/work regresses it (float4 C5, fewer-threads C6, `#pragma unroll` C13 all fail), because the compiler already pipelines and the binding constraint is concurrent-warp occupancy → boost ramp, NOT per-thread load-issue ILP. The only remaining untested winning direction is cutting WEIGHT BYTES (dp4a/int8 on the packed nibbles), which both reduces DRAM traffic AND frees registers — the genuine hard wall. Combined with (a)/(b) above, the CHEAP decode-lever space across ALL e27 models is now EXHAUSTED (qwen near-parity via C1/C2/C3 graphs; gemma-26b MoE fusion-exhausted C7-C12 + matvec occupancy-walled C5/C6/C13; gemma-31b dense BW-saturated + Q6_K-V + fast LM-head). Next cycle = the dp4a/int8 weight-traffic expert-matvec rewrite (multi-cycle, high regression risk) or accept the effort's wins as the ceiling for cheap levers.**
- **CYCLE 12 (2026-06-15) — ❌ LOGGED NEGATIVE (code REVERTED, tree clean, doc-only commit): stacking TWO more static launch-fusions on the gemma-26b MoE decode path is NEUTRAL (in-noise, ~+1.4% paired median but dead-even/slightly-negative absolute, 4/16 fail-to-boost rounds).** Continued the C7/C8/C11 static-launch-fusion lever, stacking the next two block-count-preserving candidates in `moeFfnBlock`'s batched DECODE branch (aggregate ≥2 to clear the boost floor): **(1) fold the routed GeGLU into the gate+up dual-matvec epilogue** — renamed `dmmv_q4k_experts_dual`→`dmmv_q4k_experts_geglu`; each block already produces one `(gate,up)` scalar pair at `tid==0`, so the element-wise `gelu(gate)*up` folds into that epilogue → writes `geglu_buf[e*M+row]` DIRECTLY, eliminating BOTH the separate `geglu` launch (was `ceilDiv(nrows,64)` blocks) AND the `gate_buf`/`up_buf` round-trip; **(2) drop the `zero_vec(moe_out_buf)` launch** — made `moe_weighted_acc_scaled` OVERWRITE (`a[i]=sum`, was `+=`) since the decode site computes the full weighted sum in one launch (`0+sum==sum` → the preceding zero is redundant; the `moeRoutedCombine` per-slot/prefill site keeps its own `zero_vec` so it stays correct under the now-overwriting kernel). **Change (`kernels.cu` +35/−21 = `dmmv_q4k_experts_geglu` rename+GeGLU-fold + the acc overwrite; `forward_cuda_gemma.zig` +12/−10 in `moeFfnBlock` decode branch only):** the GeGLU fold is byte-for-byte the `geglu` kernel (same `k`, same float ops on `sg,su`), the dequant arithmetic is unchanged from `dmmv_q4k_experts`, and the slot-major `e*M+row` indexing matches the old `geglu_buf` layout → BIT-IDENTICAL. Block-count PRESERVED (the C8/C11 win-class, NOT C10's collapse): the dominant matvec keeps its `nrows` blocks and the acc keeps its `ceilDiv(n_embd,64)` blocks; only two SEPARATE small launches are eliminated. **Correctness PASS: full `scripts/validate_catalog.sh` 5/5 token-correct on the cycle binary (gemma4-26b 12/12 on the fused path; gemma4-31b the documented teacher-forced near-tie); AND gemma-26b generated text token-IDENTICAL base(md5 ab42b679=C11 HEAD) vs cycle(md5 21ece624) over 40 tokens (full-output diff empty).** **A/B 4090-pinned interleaved, gemma-26b NGEN=300, TWO independent 8-round batches (16 rounds, both GPUs idle/uncontended):** batch1 ratios {1.079,0.994,1.325,1.121,1.045,0.842,1.019,0.790} median +3.2%; batch2 ratios {0.822,1.012,1.196,0.913,1.143,0.813,0.964,1.015} median ~−1%; **combined 16-round paired median +1.36% / mean +0.57%, abs medians 67.57→67.75 tok/s (+0.3%), abs MEAN 64.44→64.15 (−0.5%), CYC wins only 9/16** with **FOUR fail-to-boost rounds (ratios 0.79/0.81/0.82/0.84 — cyc pinned ~56-59 while base held ~65-72)**. The paired median sits INSIDE the ±1% boost floor and the absolute medians are dead-even / the mean is slightly negative — NONE of the C7/C8/C11 clean-win signature (tight high band + positive absolute median + 7-11/16 wins). **CONCLUSION: NEUTRAL/in-noise → not a validated win → reverted.** **Mechanism:** the two eliminated launches (`geglu` + `zero_vec`) were already cheap and parallel-friendly, and C7+C8+C11 already harvested gemma-26b's largest exposed per-token launch bubbles — the remaining ~40% decode idle is dominated by the expert-matvec DRAM-latency/weight-traffic stall (the C5/C6 wall), NOT recoverable launch bubble. Stacking two more tiny launch-fusions nets sub-noise, and the GeGLU fold's per-block `tanhf` epilogue may marginally serialize the matvec block retire, occasionally costing the boost ramp (the 4 collapse rounds). **LESSON (closes the C7/C8/C9/C10/C11 static-fusion arc): the cheap block-count-preserving static-launch-fusion candidate space on the gemma-26b MoE decode path is now EXHAUSTED — the wins (C7 gate+up +8%, C8 combine-tail +12%, C11 3-pre-norm +4.6%) drained the large exposed bubbles; the further candidates net neutral (C9 Q8_0 shared dual, C12 GeGLU+zero fold) or negative (C10 router pre-norm). The decode path is now ~3 fused launches leaner per MoE layer and the residual ~37% gap to llama is the expert-matvec weight-traffic wall (gathered/dp4a/int8 — the hard rewrite), which no launch-level fusion can touch. Next cycle should EITHER attempt that weight-traffic kernel rewrite OR pivot to a different model's decode path (e.g. scan dense gemma-31b for redundant-reduction fusions per C11's lesson).**
- **CYCLE 11 (2026-06-15) — ✅ VALIDATED WIN: static-fuse the gemma-26b MoE *3 pre-norms off `hidden`* into ONE launch → decode +4.6% (branch `perf/e27-moe-prenorm-triple` commit `b1a1bfb1`, based on C8, pushed NOT main).** Re-examined the "static-fusion exhausted" claim from C9/C10 and found a missed BLOCK-COUNT-PRESERVING candidate (the C8 win-class, NOT the C10 lose-class): in `moeFfnBlock`, `self.hidden` is rms-normalized **THREE times per MoE layer over the IDENTICAL `Σhidden²` reduction** — `rms_norm(hidden,wfn)`→ffn_norm_buf (shared expert), `rms_norm_noweight(hidden)`→norm_buf (router), `rms_norm(hidden,wpre2)`→moe_norm_buf (routed experts) — and `hidden` is unchanged until the combine tail, so all three reductions are redundant. **Change (`kernels.cu` +35 = new `rms_norm_triple`; `forward_cuda_gemma.zig` +12/−3 in the decode `moeFfnBlock`: emit all three normed buffers up front in the shared-expert command, delete the router `rms_norm_noweight` (#7) and routed `pre_ffw_norm_2` (#11) launches):** one block (grid `{1,1,1}`, 256 threads, mirrors `rms_norm`) reduces `ss=Σhidden²` ONCE, `rinv=rsqrt(ss/N+eps)`, then `xr=hidden[i]*rinv` → `y1=w1*xr` / `y2=xr` / `y3=w3*xr`. **BIT-IDENTICAL:** `ss`/`rsqrtf` are byte-for-byte the originals' and `w*(x*rinv)`/`x*rinv` are the exact left-to-right evals of `rms_norm`/`rms_norm_noweight` — the only removed work is 2 redundant full `hidden` reads + 2 redundant reductions (an identity). Single-block like all three originals → in-flight block-count PRESERVED (the C8 win-class; this is NOT C10, which collapsed the *multi-block* `mul_vec_scaled` 36→1 and regressed). Decode-path only (`moeFfnBlock`); the per-slot fallback (`moeRoutedCombine`) + batched-prefill paths keep their separate norms. **Correctness PASS: gemma-26b GEN_IDS token-IDENTICAL to base (40 tok), full `scripts/validate_catalog.sh` 5/5 token-correct (gemma4-26b 12/12 on the fused path; gemma4-31b the documented teacher-forced near-tie).** **A/B 4090-pinned interleaved, base(md5 a26b9c95 = C8 HEAD) vs cycle(md5 13d9dd7f), gemma-26b NGEN=300, TWO independent 8-round batches (16 rounds, both GPUs idle/uncontended):** batch1 ratios {1.052,1.051,0.988,1.083,1.120,1.027,0.997,1.042} median **+4.7%**; batch2 (noisier boost) {0.998,1.038,1.050,0.810,1.350,0.995,1.052,1.475} median **+4.4%**; **combined 16-round paired median +4.6%, abs medians 66.16 → 68.33 tok/s (+3.3%), CYC wins 11/16** (the 5 non-wins = 4 near-ties ≥0.988 + 1 isolated CYC boost-dip 0.810 in b2-R4, NOT systematic). CYC tracks-or-beats base across every boost regime (clearly higher in the low-boost rounds 1–2 of b1 where both arms sit ~53). **vs fresh llama-bench tg128 = 108.69 ± 6.65 t/s** same 4090+gguf → gemma-26b decode **61% (base) → 63% (cycle) of llama.** **CONCLUSION: VALIDATED WIN → committed + pushed.** **LESSON (refines C8/C9/C10): the static-fusion candidate space was NOT exhausted — the win-class extends beyond "fuse adjacent same-input matvecs/ops" to "fuse REDUNDANT reductions of the same unchanged input computed at different points in the block." Three single-block rms reductions of `hidden` were hiding in three *separate command buffers* (shared/router/routed); hoisting them into one up-front launch is pure win (fewer launches AND fewer DRAM reads), bit-identical, block-count-preserving. Next: scan the dense gemma-31b decode path (separate forward) for the same redundant-reduction pattern; the deeper ~37% gap to llama stays the expert-matvec weight-traffic wall (gathered/dp4a/int8, the hard rewrite).**
- **CYCLE 10 (2026-06-14) — ❌ LOGGED NEGATIVE (code REVERTED, tree clean, doc-only commit): static-fuse the gemma-26b MoE *router pre-norm* pair (`rms_norm_noweight`+`mul_vec_scaled`, 2 launches → 1) is ~16% SLOWER.** Picked the C9-flagged "right profile" candidate — the per-token MoE router pre-norm `norm_buf = mul_vec_scaled(rms_norm_noweight(hidden), wrscale, 1/√n_embd)`, which ran as **two tiny n_embd-sized launches in a strict dependency chain** before the router matvec. Same static-launch-fusion lever-class as the C7/C8 wins. **Change (`kernels.cu` +28 = new `rms_norm_noweight_mul_scaled`; `forward_cuda_gemma.zig` +12/−6 across BOTH decode router sites — `moeFfnBlock` + `moeRoutedCombine`; the batched-prefill `routerBatched` `mul_vec_scaled_batched` path untouched):** one kernel (grid `{1,1,1}`, 256 threads, mirrors `rms_norm_noweight`) reduces `ss=Σx²`, `rinv=rsqrt(ss/N+eps)`, then `y[i]=x[i]*rinv*w[i]*scale`. **BIT-IDENTICAL:** the `ss`/`rsqrtf(ss/N+eps)` are byte-for-byte `rms_norm_noweight`'s and `((x*rinv)*w)*scale` is the exact left-to-right eval of `mul_vec_scaled(a=x*rinv)` — the only removed work is the intermediate `x*rinv` store+reload (an identity). **Correctness PASS: gemma-26b GEN_IDS token-IDENTICAL to base (40 tok), full `scripts/validate_catalog.sh` 5/5 token-correct (gemma4-26b 12/12 on the fused path; gemma4-31b the documented teacher-forced near-tie).** **A/B 4090-pinned interleaved, base(md5 eb5c06da = C8/C9 HEAD) vs cycle(md5 dda13a4b), gemma-26b NGEN=300, 8 rounds (uncontended, both GPUs idle):** base {61.13,62.81,44.41,53.00,48.28,63.62,57.70,61.70} median **59.42**; cyc {48.50,48.65,49.56,47.20,52.75,48.01,49.63,50.73} median **49.11**; ratios 0.793/0.775/1.116/0.891/1.093/0.755/0.860/0.822 → CYC wins only **2/8** (rounds 3+5, both cases where BASE *dipped* to 44/48, NOT CYC rising); **paired median ≈ 0.84 (~16% slower); absolute medians 59.42 → 49.11 tok/s (~17% slower).** CYC is pinned in a TIGHT 47.2–52.75 band and **NEVER reaches base's 61–64 high-boost band (base hits it 4/8 rounds)** — the **C5/C6 bimodal-fail-to-boost signature, the INVERSE of the C7/C8 tight-HIGH-band + 7–8/8-wins win signature.** base ~59.4 ≈ **55% of llama** (C8's fresh tg128 = 107.61 same 4090+gguf). **CONCLUSION: NEGATIVE → reverted.** **Mechanism (why this fusion regresses where C7/C8 won):** the fusion collapses the `mul_vec_scaled` multiplication — which previously launched as `ceilDiv(n_embd,64)≈36` blocks × 64 threads (high cross-SM occupancy) — into `rms_norm_noweight`'s SINGLE block of 256 threads (grid `{1,1,1}`). It saves one launch but cuts the per-token in-flight **block-count 36→1**. In gemma-26b's full-boost-but-boost-sensitive decode regime, fewer concurrent blocks/token → the GPU fails to ramp boost (CYC pinned ~49 vs base reaching ~63), and the lost parallelism/boost-headroom costs FAR more than the single saved launch-bubble — exactly the C5/C6 weight/occupancy wall, just triggered by collapsing block-count rather than thread-count. **LESSON (refines C7/C8): static launch-fusion on gemma-26b decode only WINS when it PRESERVES the in-flight block-count — C7 fused two MULTI-block matvecs into ONE MULTI-block matvec (block-count preserved), C8 fused three already-SINGLE-block rms-style ops (no parallelism to lose). It LOSES when it collapses a MULTI-block launch into a SINGLE-block kernel (this cycle: 36→1), trading launch-bubble for occupancy/boost-ramp. The router pre-norm was the wrong target precisely because `mul_vec_scaled` was multi-block. The cheap block-count-preserving static-fusion candidates are now EXHAUSTED (C7 gate+up dual + C8 combine tail = the two wins; C9 Q8_0 shared dual = neutral, C10 router pre-norm = negative). Remaining gemma-26b decode lever stays the expert-matvec weight-traffic wall (gathered/dp4a/int8, the hard rewrite).**
- **CYCLE 9 (2026-06-14) — ❌ LOGGED NEGATIVE (code REVERTED, tree clean, doc-only commit): static-fuse the gemma-26b MoE *shared-expert* gate+up Q8_0 matvecs (2 launches → 1) is NEUTRAL/slightly-negative.** Picked the next C7/C8-flagged static-fusion candidate — the **Q8_0 shared-expert gate+up dual** (the gemma-26b shared expert's `ffn_gate.weight` + `ffn_up.weight` are both Q8_0, `sf=2112` rows each, reading the SAME `ffn_norm` input). Applied the exact C7 same-input-matvec-pair recipe to Q8_0: **Change (`kernels.cu` +37 = new `dmmv_q8_0_fast_dual`; `forward_cuda_gemma.zig` +12/−2 in `moeFfnBlock`'s shared-expert sub-block, gated on both being Q8_0 else two-launch fallback):** one kernel (grid `{sf,1,1}`, 64 threads, mirrors `dmmv_q8_0_fast`) computes BOTH gate+up per row, reading x's float4s ONCE per 32-block iter shared between the two accumulators; two `zinc_block_reduce_sum` with a `__syncthreads()` between (sh[] reuse). Each accumulator's dequant is byte-for-byte `dmmv_q8_0_fast`'s → bit-identical (both paths overwrite, acc_mode=0). Decode-path only (`moeFfnBlock`); the batched prefill (`moeSharedExpertBatched` GEMM) + per-slot paths untouched. **Correctness PASS: gemma-26b GEN_IDS token-IDENTICAL to base (40 tok), full `scripts/validate_catalog.sh` 5/5 token-correct (gemma4-26b 12/12 on the fused path; gemma4-31b the documented teacher-forced near-tie).** **A/B 4090-pinned interleaved, base(md5 8f79bba7 = C8 HEAD) vs cycle(md5 e8ecb4d3), gemma-26b NGEN=300, 8 rounds:** ratios 0.877/0.994/1.057/0.983/0.995/0.976/0.996/1.020 → CYC wins only **2/8**; **paired median ≈ 0.994 (~0.6% slower); absolute medians base 65.86 → cycle 64.72 tok/s (~1.7% slower).** In the high-boost band (rounds 4–8, both 63–70 tok/s) CYC consistently tracks base or sits slightly below (−2.4%..+2.0%) — **NONE of the C7/C8 win signature** (tight high band + 7–8/8 wins); base itself reaches the ~67 high band in 5/8 rounds and CYC never pulls a tighter/higher band above it. base ~65.86 ≈ **61% of llama** (C8's fresh tg128 = 107.61 same 4090+gguf). **CONCLUSION: NEUTRAL → not a validated win → reverted.** **Mechanism (why this Q8_0 dual fails where C7's Q4_K routed-gate+up dual won):** the dual halves the grid block-count (`sf=2112` blocks each doing gate+up, vs `2×2112` separate-launch blocks). For the shared expert's LARGE Q8_0 matvecs (8-bit weights = heavy DRAM traffic, 2112×n_embd each) the kernel is **weight-traffic/DRAM-latency-bound** (the C5/C6 wall), so cutting the in-flight block-count in half reduces memory-level parallelism (fewer concurrent blocks hiding DRAM latency) by about as much as the single saved launch-bubble buys → net wash. C7's routed gate+up won because the routed-expert matvec's per-launch overhead was a larger fraction of its work and block-count wasn't its binding constraint; the shared expert is one big launch whose latency-hiding parallelism IS the constraint. **LESSON (refines C7): the same-input matvec-pair dual fusion is NOT universal — it wins on the small/launch-dominated routed-expert path (C7) but is neutral on the large latency-bound shared-expert Q8_0 path, where halving the block grid costs the MLP it saves in launches. Static launch-fusion only helps when the launch bubble — not the matvec's own occupancy/MLP — is the binding cost. Remaining static-fusion candidates: router pre-norm (`rms_norm_noweight`+`mul_vec_scaled`, 2→1, both tiny → launch-dominated, the right profile); the deeper ~42% gap stays the expert-matvec weight-traffic wall (gathered/dp4a, hard).**
- **CYCLE 8 (2026-06-14) — ✅ VALIDATED WIN: static-fuse the gemma-26b MoE combine tail (3 launches → 1) → decode +12% (branch `perf/e27-moe-combine-tail` commit `824f7f55`, based on C7, pushed NOT main).** Picked the first C7-flagged next-candidate: the per-token MoE combine tail `hidden += post_ffw_norm(shared + moe)`, which ran as **three tiny n_embd-sized launches in a strict dependency chain (all inter-launch bubbles exposed):** `scale_accumulate(shared += moe)` + `rms_norm(shared, wpost)` + `scale_accumulate(hidden += shared)`. Same lever-class as C7 (STATIC launch-fusion — the right tool for gemma-26b's full-boost, ~40%-idle, mildly-launch-bound decode regime; graphs C4 and thread-geometry C5/C6 are wrong here, proven). **Change (`kernels.cu` +28 = new `moe_combine_tail`; `forward_cuda_gemma.zig` +6/−8 across BOTH `moeFfnBlock` variants' batched/decode branch):** one kernel (grid `{1,1,1}`, 256 threads, mirrors `rms_norm`) reduces `ss = Σ(shared+moe)²`, `rinv = rsqrt(ss/N+eps)`, then `hidden[i] += w[i]*((shared[i]+moe[i])*rinv)`. **BIT-IDENTICAL:** `t = shared[i]+moe[i]` is recomputed in both passes and never stored back; `scale_accumulate` with scale=1.0 is an exact add (`x + 1.0f*y == x + y`), and the `zinc_block_reduce_sum`/`rsqrtf(ss/N+eps)`/`w*(x*rinv)` are byte-for-byte `rms_norm`'s f32 ops — the only removed work is the intermediate f32 store+reload (an identity). Decode-path only; the prefill + per-slot fallback paths keep the original 3-launch chain. **Correctness PASS: gemma-26b GEN_IDS token-IDENTICAL to base (40 tok), full `scripts/validate_catalog.sh` 5/5 token-correct (gemma4-26b 12/12 on the fused path; gemma4-31b the documented teacher-forced near-tie).** **A/B 4090-pinned interleaved, base(md5 b856f835 = C7 HEAD) vs cycle(md5 cd125d5d), gemma-26b NGEN=300, 8 rounds:** ratios 0.784/1.473/1.474/1.315/0.997/1.074/1.126/1.149 → CYC wins **7/8** (1 low round + 1 tie); **paired median +13.75%**; **absolute medians 55.89 → 62.74 tok/s (+12.3%)** — CYC holds a TIGHT 60.3–66.7 band while base is bimodal 43.7–62.1 (same high-band-reliability signature as C7, the INVERSE of C5/C6's bimodal-LOW failure). **vs fresh llama-bench tg128 = 107.61 ± 5.27 t/s** same 4090+gguf → gemma-26b decode **52% (base) → 58% (cycle) of llama.** **CONCLUSION: VALIDATED WIN → committed + pushed.** Confirms C7's lesson: stacking static launch-fusions on gemma-26b's exposed per-token bubbles is the repeatable lever (C7 +8% on gate+up, C8 +12% on the combine tail). NEXT static-fusion candidates still open: the router pre-norm (`rms_norm_noweight`+`mul_vec_scaled`, 2→1), a Q8_0 shared-expert gate+up dual, and folding the shared-expert down+sigmoid-gate. The deeper gap to llama (still ~42%) remains the expert-matvec weight-traffic wall (gathered/dp4a, hard).
- **CYCLE 7 (2026-06-14) — ✅ VALIDATED WIN: static-fuse the gemma-26b MoE routed gate+up matvecs into ONE launch → decode +8–11% (branch `perf/e27-moe-gate-up-dual` commit `d7abe6e5`, pushed NOT main).** PROFILE FIRST (fine sampling restricted to the decode window, refining C4's coarse reading): gemma-26b MoE decode runs at **FULL boost — 2700–2715 MHz — but only ~55–65% util / ~160–185 W of 450 W**, i.e. ~40% idle at max clock. This is the KEY distinction from qwen36-35b-a3b (C2, boost-STARVED 350 MHz/18 W → graph won +62% via the boost-multiplier): gemma is NOT boost-starved, so C4's graph netted neutral — its per-token re-instantiate/update cost (expert ids change) cancelled the bubble it removed, with no boost headroom to amplify. The right lever for a full-boost, mildly-launch-bound model is **STATIC launch-fusion** (removes bubbles with ZERO per-token overhead, unlike the graph). Applied the Effort-23 same-input-matvec-pair playbook (proven +1.30% on dense via `dmmv_q4k_fast_dual`) to the MoE expert path — genuinely untried (C5/C6 were thread-geometry, C4 was the graph). **Change (`kernels.cu` +73 = new `dmmv_q4k_experts_dual`; `forward_cuda_gemma.zig` +6/−4):** the fused gate_up expert tensor stores gate (rows base 0) + up (rows base gu_half) contiguously per expert, both Q4_K sharing the SAME pre-ffn-norm input (x_stride=0). The dual kernel computes BOTH for each (expert,row) in ONE launch, reading the x float4s ONCE per superblock iteration (shared between the gate+up accumulators) instead of once per separate launch; two `zinc_block_reduce_sum` with a `__syncthreads()` between (sh[] reuse). Each accumulator's dequant is byte-for-byte `dmmv_q4k_experts` → bit-identical. Swapped the two `dmmv_q4k_experts` launches (gate base 0 + up base gu_half) in `moeFfnBlock`'s batched (decode) branch for one `dmmv_q4k_experts_dual`; the per-slot fallback + `moeRoutedCombine` + prefill paths keep the original kernel. **Correctness PASS: gemma-26b GEN_IDS token-IDENTICAL to base (40 tok), full `scripts/validate_catalog.sh` 5/5 token-correct (gemma4-26b 12/12 on the dual path; gemma4-31b the documented teacher-forced near-tie).** **A/B 4090-pinned interleaved, base(md5 4bf5500b) vs cycle(md5 a240f081), gemma-26b NGEN=300, 8 rounds:** CYC wins **8/8** — ratios 1.435/1.027/1.334/1.057/1.204/1.018/1.015/1.156 → **paired median +10.7%** (range +1.5%..+43.5%); **absolute medians 55.56 → 60.14 tok/s (+8.2%)**, and CYC holds a TIGHT 57.9–64.2 band while base is bimodal 43.4–62.4 (CYC reliably reaches the high-boost band base only hits intermittently — the INVERSE of C5/C6's bimodal-LOW failure signature). **vs fresh llama-bench tg128 = 104.82 ± 11.25 t/s** same 4090+gguf → gemma-26b decode **53% (base) → 57% (cycle) of llama.** **CONCLUSION: VALIDATED WIN → committed + pushed.** LESSON: gemma-26b's ~40% decode idle is part recoverable launch-bubble (this fusion) + part DRAM-latency stall in the weight-bound matvecs (unfixable by fusion — the C5/C6 weight-traffic wall). Static fusion is the lever C4/C5/C6 missed: graphs are wrong for full-boost models (re-instantiate tax, no boost-multiplier), thread-geometry is wrong (weight-latency-bound), but removing launches statically is free and helps. NEXT static-fusion candidates on this path: the combine tail (`scale_acc`+`rms_norm`+`scale_acc`, 3→1), the router pre-norm (`rms_norm_noweight`+`mul_vec_scaled`, 2→1), and a Q8_0 shared-expert gate+up dual; aggregate ≥2 to stay above the boost floor. The deeper gap to llama (still 43%) remains the expert-matvec weight-traffic wall (gathered/dp4a, hard).
- **CYCLE 6 (2026-06-14) — ❌ LOGGED NEGATIVE (code REVERTED, tree clean, doc-only commit): warp-per-row (32-thread) decode geometry for the 3 gemma-26b MoE expert matvecs is ~5–14% SLOWER.** Followed up CYCLE 5's lesson ("the real win must RAISE per-thread MLP — more in-flight loads — NOT reduce trip count") by testing the inverse of C5's float4 change: **halve the threads-per-row from 64→32** on the gemma-26b decode expert dispatches (`dmmv_q4k_experts` gate+up and `dmmv_q5_1_experts` down at `forward_cuda_gemma.zig:1500-1510`), so each thread strides 2× more elements/row (2× per-thread trip count = more in-flight weight loads per thread = more MLP) AND the per-row reduction drops from a `__syncthreads`/shared `zinc_block_reduce_sum` to a warp-only `zinc_warp_reduce_sum`. **Change (`kernels.cu` +6/−2, `forward_cuda_gemma.zig` +10/−3, decode-only — both kernels keep the 64-thread block-reduce for the prefill `moeRoutedCombine` path and qwen via a `blockDim.x > 32u` branch).** **Correctness PASS: catalog 5/5 token-correct, gemma4-26b 12/12 on the warp-per-row path** (gemma4-31b the documented teacher-forced near-tie; warp-reduce is bit-equivalent to block-reduce for ≤32 lanes). **A/B 4090-pinned interleaved, SAME methodology, base(md5 7fe4a8ca) vs c6(md5 7d97b8a8), gemma-26b, NGEN=300, 8 rounds:** ratios 1.324/0.758/0.753/0.706/0.992/0.958/0.932/1.010 → **median ≈ 0.945 (~5.5% slower); absolute medians base ~49.5 → c6 ~42.4 (~14% slower).** Same **bimodal fail-to-ramp** signature as C5: C6 sits pinned ~42 tok/s in 6/8 rounds and only twice catches a boost to ~52-55 (and the one big-ratio round 1 = base happened to be low at 41.65, NOT c6 being fast). BASE reaches its ~54-58 high-boost band in 4/8 rounds; C6 essentially never holds it. **vs llama-bench tg128 ≈103 t/s same 4090+gguf (C5 reading) → base ~50% of llama.** **CONCLUSION: NEGATIVE → reverted.** Mechanism: re-tiling thread geometry does NOT fix this kernel either direction — C5 (4× FEWER iters via float4) regressed AND C6 (2× MORE iters via half the threads) regressed. Halving threads/row halves the warps cooperating per row and the concurrent memory requests in flight *per row at any instant*, hurting occupancy/parallelism in a DRAM-latency-bound matvec more than the extra per-thread trip count helps; it also can't ramp boost (stuck ~42 vs base ~54). **LESSON (refines C5): the gemma-26b expert-matvec bottleneck is NOT addressable by thread-tiling at all — both reducing AND increasing per-thread trip count regress. The lever must cut WEIGHT-traffic bytes (dp4a/int8 on packed nibbles) or improve L2 residency (gathered/grouped expert layout, llama-style), not redistribute the existing fp-dequant work across threads. Stop re-litigating thread geometry on these dmmv_*_experts kernels.**
- **CYCLE 5 (2026-06-14) — ❌ LOGGED NEGATIVE (code REVERTED, tree clean, doc-only commit): float4-vectorized `dmmv_q5_1_experts` (gemma-26b MoE down-proj) is token-correct but ~15% SLOWER.** Picked CYCLE 4's flagged remaining lever — gemma-26b MoE **expert-matvec efficiency** (the worst gap, the model is only mildly launch-bound so graphs/async are dead → the matvec kernel itself is the lever). Targeted the down-proj kernel `dmmv_q5_1_experts` (used at `forward_cuda_gemma.zig:1506` for the 8 routed experts/layer, Q5_1; gate/up are Q4_K via `dmmv_q4k_experts`). **Change (only `kernels.cu`, +24/−7):** rewrote the per-thread inner loop to process the row **4 elements at a time** — one `float4` x-load (`xv[j]`), the per-Q5_1-block header (`d`/`m`/`qh`) decoded ONCE per 4 elements instead of once per element, and the 4 packed nibbles fetched via ONE aligned `uint32` read of `qs` (was 4 byte reads). Per-element dequant value is byte-for-byte the scalar kernel's; only the accumulation grouping is reassociated. **Correctness PASS: catalog 5/5 token-correct, gemma4-26b 12/12 on the new path** (gemma4-31b the documented teacher-forced near-tie). **A/B 4090-pinned interleaved, SAME methodology, base(md5 6bb22a2d) vs c5(md5 9deba1be), gemma-26b, NGEN=300, 8 rounds:** ratios 0.78/0.75/0.999/1.004/0.749/0.722/0.949/0.929 → **median ≈ 0.85 (~15% slower); C5 NEVER beats base** (best rounds only tie). BASE stable ~57 tok/s; C5 **bimodal** ~43 or ~57 (occasionally catches a boost to match base, usually sits ~43). **vs fresh llama-bench tg128 = 103.39 ± 9.95 t/s** same 4090+gguf → gemma-26b decode BASE ~55% of llama (higher than the doc's older 31% reading; current baseline is faster). **CONCLUSION: NEGATIVE → reverted.** Mechanism: the down-proj matvec is **weight-traffic/latency-bound**, not ALU-bound on the header decode — float4 does NOT reduce the dominant weight-nibble bytes read (same Q5_1 qs reads), only trims the tiny x-load + redundant header ALU. Meanwhile grouping 4 elements/iteration **cuts the loop trip-count 4×**, which reduces the number of independent in-flight loads per thread → **worse memory-latency hiding** (MLP) in a latency-bound kernel, plus added register/ILP complexity per iteration → net regression, and it interacts badly with boost (C5 fails to ramp). **LESSON: for these per-expert dequant matvecs the bottleneck is weight-read latency, not the per-element dequant ALU — float4-x vectorization is the WRONG lever (it optimizes the cheap operand). The real matvec win must cut WEIGHT traffic (dp4a/int8 on the packed nibbles, or a gathered-expert layout that keeps each expert's weights L2-resident across its tokens like llama), and/or RAISE per-thread MLP (more loads in flight), NOT reduce trip count. Also: down is only 1 of 3 expert matvecs (gate/up Q4_K dominate ~2/3) so even a real down-only win is ≤1/3 of the expert path — a future cycle should target the Q4_K gate/up path or all three together.**
- **CYCLE 4 (2026-06-14) — ❌ LOGGED NEGATIVE (code REVERTED, tree clean, NOT committed): gemma-26b-a4b MoE decode CUDA graph is NEUTRAL.** Picked the headline gap (gemma-26b MoE, 31% of llama, the worst). **PROFILE FIRST refuted the memory's "boost-saturated" label:** gemma-26b MoE decode runs at **~52% util / 157 W of 450 W / 2715 MHz** on the 4090 (single-stream async, batched experts read ids GPU-side with NO host readback) — it is only MILDLY launch-bound, NOT the deeply boost-STARVED regime (18 W / 350 MHz) that made the qwen36-35b-a3b MoE graph win +62% in C2. The gemma forward (`forward_cuda_gemma.zig`) had no graph infra, so I **ported the qwen C2 graph machinery to ForwardGemma** (+148/−15, ONLY that file): `graph`/`capturing` fields, pinned `host_embed`/`host_tok`, `submit` capture short-circuit, `gemma_attention` shared-mem fixed to `max_ctx*4` under capture, `moeGraphCapturable` (all MoE layers gate_up-Q4_K + down-Q5_1 = the existing `batched` gate), `decodeStepGraph` (embed-H2D → layer loop → tail → argmax-D2H captured, re-captured per token, instantiate-or-update + 1 launch), DEFAULT-ON for capturable MoE / OPT-IN for dense gemma (boost-saturated → forbidden). **Built isolated-cache (md5 7e206839 ≠ baseline c4e850d3); catalog 5/5 token-correct with the graph default-on** (gemma4-26b 12/12 on the graph path; gemma4-31b the documented teacher-forced near-tie). **The graph is bit-correct: gemma-26b GEN_IDS graph vs `ZINC_CUDA_GRAPH=0` were token-IDENTICAL (160 tokens) on all 5 A/B rounds.** **A/B 4090-pinned interleaved, SAME binary, default(graph)=ON vs `ZINC_CUDA_GRAPH=0`=OFF(=pre-cycle async), NGEN=160, 5 rounds:** ratios 0.747/1.012/1.374/1.032/0.899 → **median 1.012**; ON tok/s {44.85,46.44,59.95,45.37,42.35} **median 45.37** vs OFF {60.02,45.87,43.62,43.98,47.10} **median 45.87** — dead-even, with the two ~60 outliers landing in OPPOSITE arms (boost noise). **NGEN=500 confirm (decode tok/s, 2 rounds before stop):** ON 42.97/43.93 vs OFF 43.28/59.55 — ON pinned ~43-44 (low variance), OFF swings 43-60 and the async path occasionally boosts to ~59 the graph **never reaches**. **CONCLUSION: NEUTRAL → not a validated win → reverted.** Mechanism: the +62% MoE-graph win requires the DEEPLY launch-bound/boost-STARVED regime where the GPU ~idles between launches; gemma-26b's GPU is doing real work at 52% util/157W, so there's little launch-bubble to recover and the per-token capture+instantiate of gemma's large (~180-command) MoE graph cancels it (if anything it caps the boost tail). **LESSON: the MoE/dense CUDA-graph win is gated on how launch-bound the model actually is (boost-starved 18 W → win; mild 52%-util bubbles → neutral), NOT merely sub-100% util — refines C2/C3 size-gating. Practical "don't graph gemma" guidance HOLDS for gemma-26b too, now for a measured reason (insufficient bubble headroom vs graph overhead), not the inaccurate "boost-saturated" label.** Remaining gemma-26b lever = expert-matvec efficiency (gathered-expert / dp4a kernel vs the per-expert f32-dequant dmmv), the hard rewrite — NOT graphs/async.
- **CYCLE 3 (2026-06-14) — ✅ VALIDATED WIN: default-on dense decode CUDA graph for SMALL dense models → qwen35-9b decode +7.7% (branch `perf/e27-dense-graph-default` commit `1377a266`, pushed NOT main).** C2 made the MoE graph default-on; the DENSE decode graph (Effort-25 proof, +8-12% on 9b) was still **opt-IN** — so normal `gen` decode left it on the table. **Change (only `forward_cuda.zig`, +21/−6):** make the dense graph DEFAULT-ON, **size-gated** at `n_embd <= dense_graph_max_embd (4096)`; large dense stays opt-in via a truthy `ZINC_CUDA_GRAPH`. **The gate is load-bearing — NOT cosmetic:** a first pass defaulting it on for ALL dense REGRESSED qwen36-27b (A/B 4 rounds 0.942/0.948/0.986/1.033 → **median 0.967, −3%**). Root cause = exactly Effort-25 C4's size-gating: 27b's big per-layer matvecs (n_embd=5120, 64 layers) swamp the inter-launch bubbles the graph removes, so the per-step graph **re-instantiate** cost nets negative; 9b (n_embd=4096, 32 layers) is launch-bound so the graph wins. **A/B 4090-pinned interleaved, SAME binary, default(graph) vs `ZINC_CUDA_GRAPH=0`(off=pre-cycle) — isolates the graph w/ zero binary-diff noise. qwen35-9b NGEN=200, 10 rounds across 2 runs:** ON wins **10/10 non-losing** (one tie 0.998), ratios 1.049/1.082/1.076/0.998/1.065/1.069 + 1.078/1.183/1.121/1.121 → **pooled paired median +7.7%** (range +6.7%..+12.1%; abs ON median ~84-92 vs OFF ~81 tok/s). **vs fresh llama-bench tg128 = 101.02 ± 0.86 t/s** same 4090+gguf → qwen35-9b decode **~80% (off) → ~84-91% (graph) of llama**. **Catalog 5/5 token-correct** with the gated binary (9b graph-on, 27b graph-off; gemma4-31b the documented teacher-forced near-tie); 27b GEN_IDS bit-identical with/without graph (opt-in path verified). gemma dense (forward_cuda_gemma.zig) is a SEPARATE forward path → untouched, no regression risk. Next dense lever: gemma-31b (82%, separate path, fusion-only — boost-saturated) or the gemma-26b MoE matvec (31%, the worst, async-forbidden).
- **CYCLE 2 (2026-06-14) — ✅ VALIDATED WIN: capture MoE decode in a CUDA graph → qwen36-35b-a3b decode +62% (branch `perf/e27-moe-cuda-graph` commit `ab01da2c`, based on C1, pushed NOT main).** C1 unlocked this: making every 35b-a3b layer run the async batched-expert path with NO host readback turned the whole MoE decode step into a static stream → graph-capturable. (Effort-25 C4 found the catalog MoE non-capturable precisely because mixed-quant layers fell to a per-slot fallback that synced + read expert ids back mid-block — C1's q5k/q6k experts kernels removed that fallback.) **Change (only `forward_cuda.zig`, +40/−7):** new `moeGraphCapturable()` (true iff every layer's gate/up/down expert tensors have a batched experts kernel), extend the `ZINC_CUDA_GRAPH` setup gate to enable the graph for capturable MoE, and call `moeFfnBlock` (not `ffnBlock`) in `decodeStepGraph`. Kills the per-kernel launch + inter-kernel-bubble latency that left the **boost-STARVED** MoE decode idling between launches (C1 profile: ~18 W / 350 MHz). **Catalog 5/5 token-correct** both plain AND `ZINC_CUDA_GRAPH=1`; the 35b-a3b graph output is **token-identical** to non-graph (40 tokens diffed). **A/B 4090-pinned interleaved, NGEN=160, 5 rounds uncontended, base=C1 binary (no MoE graph, md5 9876dd7c) vs c2-graph (md5 e62a8498):** c2-graph wins **5/5**, ratios 1.822/1.341/1.617/1.511/1.644 → **paired median +62%** (abs medians 61.55→98.51 tok/s). c2-WITHOUT-graph (44.86/40.68) sits in base's boost-noise band (42.82/59.61) → the win is **purely the graph**, non-graph path unchanged. **vs fresh llama-bench tg128 = 106.94 ± 10.74 t/s** on the same 4090+gguf: 35b-a3b decode **40% → ~92% of llama** (the C1 baseline's 134.6 was a higher-boost llama reading; llama itself is boost-noisy here). MoE decode is now near-parity on the 4090. Note: this graph win is the boost-starved-launch-bound lever; gemma-26b MoE is boost-SATURATED so a graph there would likely be a NO-OP (Effort-25 size-gating) — its lever stays matvec-efficiency/fusion. Next: profile whether the dense models (qwen35-9b 75%, gemma-31b 82%) or the gemma-26b matvec path is the higher-EV remaining gap.
- **CYCLE 1 (2026-06-14) — ✅ VALIDATED WIN: all-quant batched MoE experts → qwen36-35b-a3b decode +27% (branch `perf/e27-moe-batched-q6k`, pushed NOT main).** PROFILE FIRST confirmed the lever: 35b-a3b decode is **boost-STARVED** — baseline 39–45 tok/s at SM **~350–450 MHz / 18 W of 450 W** (GPU idling between launches). Root cause: of 40 MoE layers, 35 take the async batched-expert path but **5 fall to the sync per-slot fallback** — gguf scan: `ffn_down_exps` = 36×Q5_K + **4×Q6_K**, `ffn_gate/up_exps` = 39×Q4_K + **1×Q5_K**. The batched gate required exactly Q4_K gate/up + Q5_K down, so the 5 odd-quant layers each did `commitAndWait` + host readback of router ids → 5 GPU drains/token that never let boost ramp (same mechanism as the [[zinc-cuda-backend]] qwen36 async win, just incomplete). **Fix:** new `dmmv_q6k_experts` kernel (Q6_K dequant from `dmmv_q6k_fast` + the `dmmv_q5k_experts` expert-addressing/16-thread-superblock layout) + generalized `moeFfnBlock` dispatch — per-tensor `expertsPipe()` picks q4k/q5k/q6k experts kernel for gate/up/down independently; batched (async, no readback) engages whenever all three have a kernel. Now all 40 layers run async. **Catalog 5/5 token-correct** (35b-a3b 12/12 with the new path; others unchanged; gemma4-31b the documented teacher-forced near-tie). **A/B 4090-pinned, interleaved base(md5 0ec3a9b4)/c1(md5 a7cc3149), NGEN=160, 5 rounds, all uncontended:** c1/base tok/s 44.51/33.44, 54.15/51.86, 57.05/44.95, 61.20/48.10, 47.92/36.33 = **c1 wins 5/5, ratios 1.331/1.044/1.269/1.272/1.319 → paired median +27%** (absolute medians 44.95→54.15, +20%); c1 max-power consistently higher (147–165 W vs 121–155 W = better boost). **vs llama.cpp same 4090+gguf (llama-bench tg128 = 134.6 t/s): 35b-a3b decode 33% → 40% of llama.** Only `forward_cuda.zig` + `kernels.cu` changed (72 insertions). Next MoE lever: the remaining 60% gap is the per-expert matvec efficiency / shared-expert launch count (llama's gathered-expert matvec), or kernel fusion in the now-async path.
